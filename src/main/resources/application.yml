spring:
  datasource:
    url: jdbc:sqlserver://localhost:1555;databaseName=NSBDWebNew
    username: feng
    password: 111111
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  context-path: /nsbdserver
